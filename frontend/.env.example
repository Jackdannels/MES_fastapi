# Optional explicit backend API target for Vite dev.
# Leave unset during local dev if you prefer using the Vite /auth and /api proxy.
# For public/static serving, use `npm run serve:public`; it proxies /auth and /api itself.
VITE_API_BASE_URL=http://127.0.0.1:8000
