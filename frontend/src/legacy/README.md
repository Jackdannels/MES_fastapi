# Legacy Runtime Archive

`frontend/src/legacy/` 已隔离为归档区。

当前约束：

- 业务页面运行不再依赖这里的运行时代码
- 该目录仅保留：
  - 历史实现参考
  - 结构/兼容测试
  - 少量用于历史数据清洗的只读字符串来源

处理原则：

- 不再从 `frontend/src` 其他业务代码导入本目录
- 如需继续删除，先移除目录内自测和历史数据清洗依赖
- 任何新增业务逻辑不得回写到本目录
