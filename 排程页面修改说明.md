# 排程页面修改说明

本文档记录本次对前端排程页面的交互、表单和甘特图显示逻辑修改。

## 修改范围

本次主要修改前端排程模块，不修改后端接口、不修改数据库结构、不改变后端保存字段。

涉及文件：

- `frontend/src/modules/schedule/page.vue`
- `frontend/src/modules/schedule/useSchedulePage.js`
- `frontend/src/modules/schedule/model.js`
- `frontend/src/modules/schedule/styles.css`
- `frontend/src/modules/schedule/model.test.js`
- `frontend/src/modules/schedule/page.runtime.test.js`

## 三天排程翻页

在甘特图三天日期表头增加翻页能力：

- 左侧单箭头：查看前三天。
- 右侧单箭头：查看后三天。
- 左侧双箭头：直接回到当前日期窗口。
- 左翻页不能早于当前日期。
- 当已经处于当前日期窗口时，左翻页和回到当前日期按钮禁用并变淡。

实现方式：

- `useSchedulePage.js` 中新增 `ganttWindowOffsetDays` 表示当前三天窗口相对今天的偏移天数。
- 点击后三天时，偏移量加 `3`。
- 点击前三天时，偏移量最多减到 `0`，不会进入今天之前。
- 点击双左箭头时，偏移量直接恢复为 `0`。
- `buildGanttRows()` 接收 `startDate`，根据当前窗口生成三天甘特图。

## 固定三天窗口

排程甘特图现在固定显示三天，不再因为跨天排程自动扩展表格天数。

注意：

- 跨天排程仍然可以存在。
- 只显示当前三天窗口内能看到的部分。
- 不会把表格自动拉长到第四天、第五天。

## 同一任务连续排程合并显示

同一个实验室行内，如果相邻半天格属于同一个任务编号，会合并成一个连续长条。

效果：

- 原来同一个任务可能在多个半天格里重复显示。
- 现在连续区间会合并显示。
- 任务编号显示在合并后的长条中间。

实现方式：

- `model.js` 中甘特图分段合并逻辑由按单个排程记录合并，调整为按任务编号合并。
- 只在当前三天窗口内合并，不改变三天窗口范围。

## 实验室行级跳转箭头

在甘特图左侧实验室列增加行级导航箭头。

显示规则：

- 只有该实验室存在“当前日期及以后”的“已排程”记录时，才显示左右箭头。
- 没有符合条件的已排程记录时，不显示箭头，保持原有实验室名称显示。
- 今天以前的已排程记录不会触发箭头。
- 待排程、暂存、进行中等非“已排程”状态不会触发箭头。

点击规则：

- 左箭头：跳到该实验室上一条已排程实验的开始日期对应页面。
- 右箭头：跳到该实验室下一条已排程实验的开始日期对应页面。
- 已经处于该实验室第一条已排程实验日期时，左箭头禁用并变淡。
- 已经处于该实验室最后一条已排程实验日期时，右箭头禁用并变淡。

布局处理：

- 没有箭头的实验室行不套额外布局，避免挤压实验室名称。
- 有箭头的实验室行只在名称左右叠加按钮，不改变其它列布局。
- 实验室列宽度调整为较紧凑但可读的固定宽度。

## 自定义排程时间限制

当时段选择“自定义”时，开始时间不能早于当前时间。

实现方式：

- 页面输入框会在当天排程时设置 `min` 为当前时间。
- `model.js` 中 `resolveScheduleTimes()` 也做了兜底校验。
- 如果绕过页面直接提交过去时间，也会返回错误。

错误提示：

```text
自定义开始时间不能早于当前时间
```

## 预计实验时长单位

当时段选择“自定义”时，预计实验时长右侧新增单位选择。

支持单位：

- 小时
- 天数

小时模式：

- 沿用原逻辑。
- 最小值 `0.5`。
- 步长 `0.5`。
- 保存时仍写入小时数。

天数模式：

- 最小值 `1`。
- 步长 `1`。
- 保存时转换为小时数写入原字段。

示例：

- `1 天` 保存为 `24` 小时。
- `2 天` 保存为 `48` 小时。
- `4 天` 保存为 `96` 小时。

重要说明：

- 新增的 `planned_duration_unit` 只存在前端表单状态中。
- 后端和持久化数据仍然使用原来的 `planned_hours` 字段。
- 因此不会影响后端接口和数据库字段。

## 删除后重新排程回填

当从已有排程执行“删除后重新排程”时，表单会根据原排程时长自动选择合适单位。

规则：

- 如果原 `planned_hours` 是 `24` 的整数倍，则回填为“天数”。
- 如果不是 `24` 的整数倍，则回填为“小时”。

示例：

- `24` 小时回填为 `1 天`。
- `48` 小时回填为 `2 天`。
- `96` 小时回填为 `4 天`。
- `26.5` 小时仍回填为 `26.5 小时`。

## 对后端交互的影响

本次修改不改变前后端接口。

仍然使用原有快照键：

- `mes.schedules`
- `mes.tasks`
- `mes.experiments`
- `mes.streams`
- `mes.conflicts`

仍然使用原有排程字段：

- `task_code`
- `experiment_code`
- `device`
- `start_at`
- `end_at`
- `planned_hours`
- `status`

没有新增后端字段。

没有新增数据库字段。

没有修改接口 URL。

没有修改后端读写流程。

## 代码审查结论

已检查本次修改对其它页面和后端交互的影响：

- 排程翻页状态仅在 `useSchedulePage()` 内部维护，不影响其它模块路由。
- 甘特图三天窗口只影响排程页面展示，不影响排程列表数据。
- 实验室行级箭头只读取前端已有 `rawSchedules`，不会触发额外后端请求。
- 时长单位只用于前端表单转换，最终仍保存为 `planned_hours`。
- 自定义时间校验在模型层兜底，不依赖单纯的页面输入限制。
- `createScheduleRecord()` 和 `updateScheduleRecord()` 的持久化结构未改变。
- 后端接口与数据库交互逻辑未被修改。

未发现会影响其它页面或后端接口契约的问题。

## 已执行验证

已执行排程模块测试：

```powershell
npm.cmd run test:run -- src/modules/schedule/model.test.js src/modules/schedule/useSchedulePage.test.js src/modules/schedule/page.runtime.test.js
```

结果：

```text
3 passed
81 passed
```

已执行前端构建：

```powershell
npm.cmd run build
```

结果：

```text
build successful
```

