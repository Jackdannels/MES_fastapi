# 高低温湿热二室当前维护说明

该文件保留为历史计划入口的当前状态说明。

高低温湿热二室（`LAB_HOT_HUMID_2`）没有上位机，该试验间仅使用 mock 模式（hostless 本地模拟）。该例外只用于替代物理上位机的 fixture-ready 和 experiment-started 信号，不代表项目仍支持全局 mock 模式。

维护要求：

- 除高低温湿热二室外，所有试验间仅使用 MQTT 模式。
- 不要删除二室 hostless 本地模拟逻辑。
- 不要为其他试验间恢复 mock 模式、mock 开始实验或 mock 完成实验入口。
- 二室本地模拟必须继续复用共享业务规则，不能复制独立状态机。
